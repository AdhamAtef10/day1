import 'package:flutter/material.dart';

class ProductTOCategory extends StatelessWidget {
  const ProductTOCategory({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Container(

          ),
        ],
      ),
    );
  }
}
